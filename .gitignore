# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Sistemas operacionais / editores
.DS_Store
Thumbs.db
.vscode/

# Dependências (ignora qualquer node_modules/ em qualquer subpasta)
node_modules/

# Builds/outputs
dist/
build/
coverage/

# Variáveis de ambiente (nunca comitar segredos)
.env
.env.local
.env.*.local

# Frontend (Vite / React) — se quiser ser explícito
react/dist/
react/.env
react/.env.local
